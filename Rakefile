require "bundler/gem_tasks"
require "rake/extensiontask"


Rake::ExtensionTask.new "lcmrun" do |ext|
  ext.lib_dir = "lib/nysol"
end
Rake::ExtensionTask.new "lcmseqrun" do |ext|
  ext.lib_dir = "lib/nysol"
end
Rake::ExtensionTask.new "lcmseq0run" do |ext|
  ext.lib_dir = "lib/nysol"
end

Rake::ExtensionTask.new "lcmtransrun" do |ext|
  ext.lib_dir = "lib/nysol"

end
Rake::ExtensionTask.new "sspcrun" do |ext|
  ext.lib_dir = "lib/nysol"
end
Rake::ExtensionTask.new "grhfilrun" do |ext|
  ext.lib_dir = "lib/nysol"
end
Rake::ExtensionTask.new "macerun" do |ext|
  ext.lib_dir = "lib/nysol"
end
